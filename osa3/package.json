{
  "name": "puhelinluettelo-backend",
  "version": "1.0.0",
  "description": "backend for puhelinluettelo application",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "buildui": "rm -rf dist && cd ../osa2/puhelinluettelo && npm run build && cp -r dist ../../osa3",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint ."
  },
  "author": "Juuso Karvinen",
  "license": "ISC",
  "devDependencies": {
    "@eslint/js": "^9.14.0",
    "@stylistic/eslint-plugin-js": "^2.10.1",
    "eslint": "^9.14.0",
    "globals": "^15.12.0",
    "nodemon": "^3.1.7"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "mongoose": "^8.8.1",
    "morgan": "^1.10.0"
  }
}
